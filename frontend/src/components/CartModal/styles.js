import styled from 'styled-components'
import { radius } from '../../styles/styleVariables'

const Container = styled.section`
  background-color: white;
  border: none;
  width: 100%;
  padding: 25px;
  border-radius: ${radius.main};
`

export { Container }
